apiVersion: v1
kind: Service
metadata:
  name: {{ include "springboot_student.fullname" . }}
  labels:
    {{- include "springboot_student.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "springboot_student.selectorLabels" . | nindent 4 }}
